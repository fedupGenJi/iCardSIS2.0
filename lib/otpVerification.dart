import 'package:flutter/material.dart';

class otpVerificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("OTP Verification")),
      body: Center(child: Text("Enter OTP here")),
    );
  }
}
